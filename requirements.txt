# AI Study Room Scheduling Agent
# The CLI demo (main.py) uses only the Python standard library.
# These dependencies are needed for the web server (server.py) only.

fastapi>=0.100.0
uvicorn>=0.23.0
